{
  "name": "test-task",
  "version": "1.0.0",
  "description": "1. Clone this repository.\r 2. Run `npm init` in the project directory.\r 3. Run `npm install` in the project directory.\r 4. Run `npx cypress run` in the project directory for running tests.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^12.11.0"
  },
  "devDependencies": {
    "cypress-mochawesome-reporter": "^3.4.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0"
  }
}
